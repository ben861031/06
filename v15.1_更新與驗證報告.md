# v15.1 更新與驗證報告

## 本次完成

1. 系統管理導覽依基礎設定、帳號與權限、系統紀錄分組。
2. 人員管理工具列、統計卡、表格、狀態與操作按鈕統一。
3. 部門及印鑑維護改為正式主檔表格，並補上排序按鈕停用狀態與刪除確認。
4. 登入紀錄加入搜尋、起訖日期、重設及中文角色顯示。
5. 登入紀錄優先顯示部門姓名，員編與 Email 作為次要資料。
6. 操作紀錄保留精簡摘要，完整內容改由詳情 Modal 顯示。
7. 登入與操作紀錄改用精簡分頁，降低頁尾按鈕擁擠。
8. 系統管理 Modal、按鈕、表格、空資料與響應式排版一致化。

## 與同系列產品對齊

- 沿用行政部及通用問卷的白底、細邊框、低陰影管理介面。
- 主要、次要、危險操作有一致層級。
- 狀態與角色以膠囊呈現，原始角色代碼改為中文。
- 名單優先顯示部門與姓名，Email 降為次要資訊。
- 長內容透過 Modal 檢視，不再讓主表格過寬。

## Firestore 規則與資料結構

- Firestore 規則內容未變更。
- 沒有新增、刪除或更名集合。
- `members`、`memberAccounts` 仍為三套系統共用人員主檔。
- `sealPermissions` 仍只管理印鑑系統角色。
- 本版沒有使用過度寬鬆的公開讀寫規則。

## 已執行檢查

- `node --check app.js`：通過。
- HTML 重複 ID 掃描：139 個 ID 均為唯一值。
- `firestore.rules` 與 v15.1 完整合併規則 SHA-256：一致。
- `index.html`、`styles.css`、`app.js`、Logo 資產本機 HTTP 載入：均為 200。
- 桌機 1440 x 900 實際渲染：登入入口正常，無水平溢出。
- 手機 390 x 844 實際渲染：登入卡與按鈕正常，無文字或按鈕破版。
- 本機瀏覽器 console：未發現 error 或 warning。
- 系統管理選單與頁面入口靜態檢查。
- 登入紀錄搜尋、日期及重設事件路徑檢查。
- 操作紀錄搜尋、分類、日期及詳情 Modal 事件路徑檢查。
- 部門及印鑑新增、排序、刪除事件路徑檢查。

## 仍需線上帳號驗證

因本機無法替代實際 Firebase 權限與正式資料，部署後請各驗證一次：

1. 全域系統管理員可使用共用人員管理。
2. 印鑑 Admin 可管理印鑑角色，但不能越權修改共用人員主檔。
3. 登入紀錄可依正式帳號資料顯示部門姓名。
4. 操作紀錄詳情可載入正式資料中的前後內容。
5. 手機瀏覽器可操作人員、部門、印鑑與紀錄頁面。

## 已知風險

- 舊登入紀錄若沒有可對應的共用人員帳號，會保留 Email 作為顯示名稱。
- 刪除部門或印鑑仍可能影響舊資料語意，正式環境建議優先停用或保留主檔。
- 本版著重介面與操作一致化，未改寫既有 Firestore 查詢與權限核心。
