# v15.3 更新與驗證報告

## 修正範圍

本版只處理手機版頁首與主導覽，不變更借用作業、系統管理功能、資料結構、權限判斷或桌機版排版。

## 問題原因

舊版手機功能列使用水平捲動容器，而功能下拉選單位於該容器內並採絕對定位。瀏覽器會把超出容器的下拉內容裁切，因此畫面只剩狹小區域可操作，並產生水平捲軸。

## 實作內容

1. 手機頁首改為品牌、登入者、主選單三層排列。
2. 主選單改為兩欄等寬 Grid，取消水平捲動。
3. 下拉選單最大寬度限制為手機可視寬度。
4. 借用作業選單靠左、系統管理選單靠右展開。
5. 下拉內容過長時只在下拉區域內垂直捲動。
6. 360px 以下收斂 Logo、標題及按鈕字級。
7. 更新 CSS 快取版本，避免 GitHub Pages 繼續載入舊樣式。

## 實際驗證

| 項目 | 結果 |
|---|---|
| 390 x 844 頁面寬度 | 通過，`scrollWidth` 與 `innerWidth` 均為 390 |
| 390 x 844 系統管理下拉 | 通過，左右邊界 60.4 至 370.4，完整位於視窗內 |
| 360 x 800 頁面寬度 | 通過，`scrollWidth` 與 `innerWidth` 均為 360 |
| 360 x 800 系統管理下拉 | 通過，左右邊界 36 至 346，完整位於視窗內 |
| 下拉內容 | 通過，可顯示並點擊至「操作紀錄」 |
| JavaScript 語法 | 通過 `node --check app.js` |
| 桌機版 | 本版規則僅位於 `max-width: 760px` 與 `max-width: 390px` 媒體查詢內 |

## Firestore

- Firestore 規則未變更。
- Firestore 索引未變更。
- 不需因本版重新部署 Firebase。

## 已知限制

本機測試環境沒有正式 Firebase 登入狀態，因此以正式頁首 DOM 與相同 CSS 建立一次性元件測試頁完成 390px、360px 視覺與邊界驗證；測試頁未納入正式輸出。
