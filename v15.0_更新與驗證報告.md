# v15.0 更新與驗證報告

## 本次完成

1. 新增與行政、通用問卷共用的人員管理頁。
2. 共用人員資料仍使用既有 `members`、`memberAccounts`，未建立重複集合。
3. 保留印鑑專屬 `sealPermissions`，不改變 Admin、User、Viewer 的實際權限。
4. 權限下拉改列出全部啟用人員，並清楚標示不可選取原因。
5. 相容 `googleEmail`、`googleAccount`、`email`、`gmail` 等舊欄位，解決啟用人員被漏列的問題。
6. 新增人員 Excel 範本、匯入、匯出及完整名單差異核對。
7. 完整名單匯入不會直接刪除未出現人員，須由管理員勾選後才停用。
8. 修改 Google 帳號時同步搬移印鑑權限，避免換帳號後失去角色。

## 原因分析

v14.9 權限下拉主要依賴 `memberAccounts` 的直接配對。部分舊人員雖為啟用狀態，但 Google 帳號仍存放在人員文件舊欄位，因此被過濾掉。本版統一由 `memberGoogleEmail()` 相容解析，並把全部啟用同仁都呈現在下拉中。

## Firestore 規則

- 規則內容未變更。
- `members`、`memberAccounts` 仍只有全域系統管理員可寫入。
- `sealPermissions` 仍依印鑑系統角色規則管理。
- 沒有使用 `allow read, write: if true`。

## 相容性

- 沒有更名或搬移既有集合。
- 舊人員、舊帳號欄位與舊印鑑權限均可繼續讀取。
- 既有借用、歸還、待借用、印鑑、部門、登入紀錄及操作紀錄流程均保留。

## 已執行檢查

- `node --check app.js`：通過。
- 重複函式名稱掃描：未發現重複宣告。
- 共用人員頁 DOM 與事件入口：已完成靜態檢查。
- 權限下拉資料來源：已確認使用啟用 `members` 並相容 `memberAccounts` 與舊 Google 帳號欄位。
- 全域系統管理員與印鑑 Admin 權限界線：已完成程式路徑檢查。

## 尚需線上驗證

因本機無法代替實際 Firebase 帳號完成資料寫入，部署後請各驗證一次：

1. 全域系統管理員可進入人員管理並修改一筆測試人員。
2. 印鑑 Admin 看得到權限管理，但看不到共用人員管理入口。
3. 權限下拉顯示全部啟用同仁，已設定與缺帳號者有正確提示。
4. 停用人員後，該人不再出現在可指派名單。
5. 完整名單匯入的未出現人員不會在未勾選時被停用。

## 已知風險

- 永久刪除人員屬高風險操作，實務上應優先使用停用。
- 若同一員工在歷史資料中使用不同員工編號或不同 Google 帳號，仍需由管理員先確認再合併或停用。
